# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'datastoreLoadTesting' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for datastoreLoadTesting
  pod 'Amplify', :path => '~/aws-amplify/temp/amplify-ios/'
  pod 'AmplifyPlugins/AWSDataStorePlugin', :path => '~/aws-amplify/temp/amplify-ios/'
  pod 'AmplifyPlugins/AWSAPIPlugin', :path => '~/aws-amplify/temp/amplify-ios/'
  
  target 'datastoreLoadTestingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'datastoreLoadTestingUITests' do
    # Pods for testing
  end

end
